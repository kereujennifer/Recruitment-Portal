@using Auth.Models.ViewModels;
@model ApplyJobViewModel;

@{
    ViewData["Title"] = "MyApplications";
}
<div class="card box-shadow m-2 p-5">
<div class="row justify-content-end">

    <div class="col-lg-2 col-md-6">
        <a class="btn primary mb-2" asp-action="Dashboard" asp-controller="Home"><i class="ri-add-line"></i>Add</a>
    </div>

</div>
    <table class="table  table-bordered" id="example">
    <thead>
        <tr>
           
            <th>
                @Html.DisplayNameFor(model => model.ReferenceNumber)
            </th>
          
            <th>
                @Html.DisplayNameFor(model => model.JobTitle)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateAdded)
            </th>
          
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.JobApplications) {
        <tr>
          
            <td>
                @Html.DisplayFor(modelItem => item.ReferenceNumber)
            </td>
           
            <td>
                @Html.DisplayFor(modelItem => item.JobTitle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateAdded)
            </td>
           
            <td>
                            <a asp-action="Details" asp-route-id="@item.Id">
                                <i class="ri-eye-line primary-text me-3" id="newsEditId" data-bs-toggle="tooltip" data-bs-placement="right"
                                   data-bs-custom-class="custom-tooltip"
                                   data-bs-title="eye"></i>
                            </a>
                    </td>
        </tr>
}
    </tbody>
</table>
</div>